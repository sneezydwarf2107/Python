def check(sudoku):
	"""This function will check the empty fields of the sudoku for possible candidates

	Arguments:
		sudoku {list} -- list containing the unsolved sudoku
	"""
	a = [[],[],[]]
	a[0] = [1,2]
	a[1] = []



	return a

def is_empty(test):

	if test:
		return False

	else:
		return True

